/*
import java.util.ArrayList;
import java.util.List;


public class Cart {

    private int itemCount;
    private double totalPrice;
    private static int capacity;
    int index=0;

    private   List<Item> items = new ArrayList<Item>();

    public int getItemCount() {
        return itemCount;
    }

    public void setItemCount(int itemCount) {
        this.itemCount = itemCount;
    }

    public double getTotalPrice() {
        return totalPrice;
    }

    public void setTotalPrice(double totalPrice) {
        this.totalPrice = totalPrice;
    }

    public static int getCapacity() {
        return capacity;
    }

    public static void setCapacity(int capacity) {
        Cart.capacity = capacity;
    }

    public int getIndex() {
        return index;
    }

    public void setIndex(int index) {
        this.index = index;
    }

    public Cart(){


        itemCount = 0;
        totalPrice = 0.0;
        capacity = 1;
       // cart = new Item[capacity];
    }
 public void add(int itemID, String itemName,double itemPrice,int itemQuantity)
 {

//     Item item = new Item();
//     totalPrice += (itemPrice * itemQuantity);
//     System.out.println("TOTAL: "+totalPrice);
////     cart[index] = item;
//     index++;

     System.out.println("Id" + "    " + "Name" + "    " + "Price" + "    " + "Qty" + " totalPrice" );

     */
/*for (Item c :cart) {
         System.out.println(c.getID() + "    " +c.getName() + "    "+ c.getPrice() + "    " +c.getQuantity()+ "    "  );

     }*//*


     */
/*itemCount += 1;
     if (itemCount == capacity) {

         increaseSize();
     }*//*

 }


//        public static void remove (String itemName){
//            Scanner s = new Scanner(System.in);
//
//
//            for (int i = 0; i < cart.length; i++) {
//                Item remove = (Item) cart.get(i);
//                if (itemName.equals(remove.getName())) {
//                    cart.remove(i);
//
//            }
//    }
//
//            System.out.println("\n" + "Item" + itemName + "wasn't found");
//
//        }

      */
/*  private void increaseSize(){
            Item[] item = new Item [capacity];
            for (int i = 0; i < capacity; i++){
                item[i] = cart[i];

            }
            cart= item;
            item = null;
           // capacity = cart.length;

        }
*//*

      public static void prLine(int itemID, String itemName , double itemPrice, int itemQuantity, double total) {

            System.out.printf("-10.10d %30s %10.2f %10d %10.2f ",itemID,itemName,itemPrice,itemQuantity,total);

        }

    public static void prTitles () {
        System.out.printf("\n%-10s 30% %10s %10s %10s %10s", "ID", "Item", "Price", "Quantity", "Total");
    }
    public void add(Item item)
    {


        System.out.println("TOTAL: "+totalPrice);
        items.add(item);


        System.out.println("Id" + "    " + "Name" + "    " + "Price" + "    " + "Qty" + "    " );

     for (Item c :items) {
         System.out.println(c.getID() + "    " +c.getName() + "    "+ c.getPrice() + "    " +c.getQuantity()+ "    "  );

     }

     */
/*itemCount += 1;
     if (itemCount == capacity) {

         increaseSize();
     }*//*

    }

}
*/
